#include<iostream.h>

void main()
{
  //assuming the input to already exist as the number n
  
  int num=1;
  do
  {
    num*=2;
    if(num>=n)
      break;
  }while(num>n);
  
  stdio::cout<<"the biggest power of 2 after "<<n<<" is :"<<num;
}
