void Solution::arrange(vector<int> &A) 
{
    int max_element = A[0];
    for(int i=1;i<A.size();i++)
    {
        if(A[i] > max_element)
        max_element = A[i];
    }
    for(int i=0;i<max_element;i++)
    cout<<A[i]<<" ";
}
