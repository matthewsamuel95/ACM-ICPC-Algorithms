#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int t;
    cin>>t;
    
    while(t--)
    {
        int n;
        cin>>n;
        int a[n];
        for(int i=1;i<=n;i++)
            a[i]=1;
        for(int i=2;i<=n;i++)
        {
            if(__gcd(n,i)!=1)
            {
            for(int j=i;j<=n;j+=i)
                a[j]=0;
            }
        }
            
            int count = 0;
            for(int i=1;i<=n;i++)
            if(a[i] == 1)
            count++;
            cout<<count<<endl;
    }
}
